## 1. Verdict
NEEDS REVISION

## 2. Finding summary
- Blocker: 0
- Major: 2
- Minor: 1
- Nit: 0

## 3. Findings by checklist item
1. Pass. The do-not-diverge paragraph is present verbatim at the top of the plan at `plans/central/LAYER_8_metrics.md:1`.

2. Partial. The plan encodes the locked behavior in `plans/central/LAYER_8_metrics.md:3`, `plans/central/LAYER_8_metrics.md:30-34`, `plans/central/LAYER_8_metrics.md:48-57`, `plans/central/LAYER_8_metrics.md:74`, and `plans/central/LAYER_8_metrics.md:121-125`: transcript-store-derived stats, no duplicate write path, `@MainActor`, published rollups, and focus/commit refresh with no polling. But it diverges from the master prompt's literal API contract. The prompt says "`MetricsService: Sendable` — read-only queries" with `recordingsThisWeek()`, `wordsThisWeek()`, `minsSavedThisWeek()`, `wpmAverageThisWeek()`, and `recentTranscriptions(limit:)` at `plans/CENTRAL_LAYERS_PROMPT.md:349-355`, and "`HomeTabViewModel` injects `any MetricsService`, calls the 4 queries on appear" at `plans/CENTRAL_LAYERS_PROMPT.md:358`. The plan instead makes `MetricsService` an `ObservableObject` with published state and moves the query surface to `MetricsReading` at `plans/central/LAYER_8_metrics.md:43-57`. The prompt also says "Author `plans/backlog/home-stats-metrics.md`" at `plans/CENTRAL_LAYERS_PROMPT.md:359`, while the plan weakens that to optional backlog tracking at `plans/central/LAYER_8_metrics.md:173-174`.

3. Partial. The document is structured as Stage 1 / Stage 2 / Stage 3 and Stage 1 is scoped to `Sources/SeshatCore/Metrics/` at `plans/central/LAYER_8_metrics.md:76-155`, so the shape matches the central-layer template. The problem is staleness against the checked-out trunk: Stage 1 already exists in `Sources/SeshatCore/Metrics/MetricsModels.swift:3-89`, `Sources/SeshatCore/Metrics/MetricsProtocols.swift:4-20`, `Sources/SeshatCore/Metrics/SQLiteMetricsReader.swift:4-129`, `Sources/SeshatCore/Metrics/SQLiteMetricsService.swift:4-153`, plus `Tests/SeshatCoreTests/Metrics/SQLiteMetricsReaderTests.swift:5-138` and `Tests/SeshatCoreTests/Metrics/SQLiteMetricsServiceTests.swift:6-208`. The plan still describes Stage 1 as future "new files" at `plans/central/LAYER_8_metrics.md:82`, `plans/central/LAYER_8_metrics.md:94`, and `plans/central/LAYER_8_metrics.md:107`.

4. Pass. The validation checklists consistently use concrete `file:line` anchors, for example at `plans/central/LAYER_8_metrics.md:84-87`, `plans/central/LAYER_8_metrics.md:96-100`, `plans/central/LAYER_8_metrics.md:109-112`, `plans/central/LAYER_8_metrics.md:121-125`, `plans/central/LAYER_8_metrics.md:134-137`, `plans/central/LAYER_8_metrics.md:151-155`, and `plans/central/LAYER_8_metrics.md:164-171`.

5. Fail. The duplicate audit is incomplete. The plan checks for overlapping debug-counter UI and non-rollup duration math at `plans/central/LAYER_8_metrics.md:127-155`, but it omits the already-landed metrics layer on trunk. Today trunk already has a metrics namespace and live read-only service in `Sources/SeshatCore/Metrics/MetricsModels.swift:3-89`, `Sources/SeshatCore/Metrics/MetricsProtocols.swift:4-20`, `Sources/SeshatCore/Metrics/SQLiteMetricsReader.swift:4-129`, and `Sources/SeshatCore/Metrics/SQLiteMetricsService.swift:4-153`, with matching tests in `Tests/SeshatCoreTests/Metrics/SQLiteMetricsReaderTests.swift:5-138`, `Tests/SeshatCoreTests/Metrics/MetricsContractTests.swift:6-62`, and `Tests/SeshatCoreTests/Metrics/SQLiteMetricsServiceTests.swift:6-208`. Because the plan's current-spread and delete-pass audits never cite those files, Stage 1 would duplicate an existing metrics service rather than only removing scattered word-count or duration math.

6. Partial. The plan obeys the collision rule in the abstract by keeping Stage 1 under `Sources/SeshatCore/Metrics/` and `Tests/SeshatCoreTests/Metrics/` at `plans/central/LAYER_8_metrics.md:78`, `plans/central/LAYER_8_metrics.md:82`, `plans/central/LAYER_8_metrics.md:103`, and `plans/central/LAYER_8_metrics.md:165`. In the current trunk those directories are already populated by tracked files at `Sources/SeshatCore/Metrics/MetricsModels.swift:3-89`, `Sources/SeshatCore/Metrics/MetricsProtocols.swift:4-20`, `Sources/SeshatCore/Metrics/SQLiteMetricsReader.swift:4-129`, `Sources/SeshatCore/Metrics/SQLiteMetricsService.swift:4-153`, and `Tests/SeshatCoreTests/Metrics/SQLiteMetricsReaderTests.swift:5-138`, so Stage 1 is no longer collision-free "new directory" work.

7. Partial. The intended design is behavior-neutral and downstream-only at `plans/central/LAYER_8_metrics.md:30-34`, `plans/central/LAYER_8_metrics.md:60`, `plans/central/LAYER_8_metrics.md:74`, and `plans/central/LAYER_8_metrics.md:149`. But the stale trunk assumptions at `plans/central/LAYER_8_metrics.md:8-10`, `plans/central/LAYER_8_metrics.md:77-107`, and `plans/central/LAYER_8_metrics.md:145-149` mean the plan can no longer be executed verbatim without first reconciling the already-landed metrics implementation on trunk.

8. Partial. The high-level Layer 2 dependency is correct in `plans/central/LAYER_8_metrics.md:63`, `plans/central/LAYER_8_metrics.md:115`, and `plans/central/LAYER_8_metrics.md:177`, and it matches `plans/CENTRAL_LAYERS_PROMPT.md:369` plus the Layer 2 plan's unblocking language at `plans/central/LAYER_2_storage.md:100` and `plans/central/LAYER_2_storage.md:208`. The inconsistency is sequencing: `plans/central/LAYER_8_metrics.md:78` says Stage 1 can run in parallel with Layer 2 Stage 1, while `plans/central/LAYER_2_storage.md:69-72` explicitly says not to start Layer 8 Stage 1 until Layer 2 Stage 1 has landed.

9. Pass. The Stage 2 consumer list is explicit and correct at `plans/central/LAYER_8_metrics.md:3`, `plans/central/LAYER_8_metrics.md:114-137`: the Home tab is the primary consumer and any real overlapping debug stats UI is a conditional second consumer. That matches the current trunk, which still has no checked-in Home tab in `Sources/SeshatAppKit/Composition/SeshatAppMain.swift:170-195` and no shipped debug stats UI in `Sources/SeshatAppKit/Notes/NotesView.swift:43-71`; the only checked-in `counter` references remain test helpers at `Tests/SeshatAppKitTests/AppStartupCoordinatorTests.swift:28-53` and `Tests/SeshatAppKitTests/AppStartupCoordinatorTests.swift:89-111`.

10. Pass. `[QUESTION]` markers are preserved at `plans/central/LAYER_8_metrics.md:183-187`.

11. Pass. The Manus Home-tab shape matches. The plan's stat list and recent-list limit appear at `plans/central/LAYER_8_metrics.md:3`, `plans/central/LAYER_8_metrics.md:67-72`, and `plans/central/LAYER_8_metrics.md:119-125`, which aligns with `plans/App UI design/Seshat UI Polish & Unified Window Implementation.md:54-57`.

## 4. Cross-layer concerns
- Layer 2 dependency is directionally correct, but the sequencing text conflicts. `plans/central/LAYER_8_metrics.md:63`, `plans/central/LAYER_8_metrics.md:115`, and `plans/central/LAYER_8_metrics.md:177` correctly defer live path wiring to Layer 2 Stage 2, and current trunk Stage 1 code also takes a raw `databaseURL` rather than a storage locator in `Sources/SeshatCore/Metrics/SQLiteMetricsService.swift:25-44`. But `plans/central/LAYER_8_metrics.md:78` says Layer 8 Stage 1 can run in parallel with Layer 2 Stage 1, while `plans/central/LAYER_2_storage.md:69-72` forbids starting Layer 8 Stage 1 before Layer 2 Stage 1 lands.
- Layer 7 makes the transcript-commit notification owner choice more constrained than the Layer 8 plan states. `plans/central/LAYER_8_metrics.md:124` and `plans/central/LAYER_8_metrics.md:185` frame the future producer as either `SessionCoordinator.persistTranscript` or `SQLiteTranscriptStore.append(_:)`. But current trunk already has pipeline-owned persistence in `Sources/SeshatSession/Pipeline/Orchestrator/SessionPipelineOrchestrator.swift:344-359`, and the Layer 7 plan makes persistence an orchestrator stage at `plans/central/LAYER_7_pipeline.md:90-99` and `plans/central/LAYER_7_pipeline.md:152-167`. Choosing `SessionCoordinator.persistTranscript` would couple Layer 8 to a seam Layer 7 intends to retire.
- Current trunk proves that Stage 2 notification wiring is still incomplete. The notification exists in `Sources/SeshatCore/Metrics/MetricsModels.swift:87-88` and the service observes it in `Sources/SeshatCore/Metrics/SQLiteMetricsService.swift:115-127`, but no shipped source currently posts it: `Sources/SeshatSession/SessionCoordinator.swift:220-241` appends without posting, and `Sources/SeshatSession/Pipeline/Orchestrator/SessionPipelineOrchestrator.swift:344-359` persists without posting. The only poster today is test-only in `Tests/SeshatCoreTests/Metrics/SQLiteMetricsServiceTests.swift:74-88`.

## 5. Stale file:line citations
- None of the explicit `file:line` anchors in `plans/central/LAYER_8_metrics.md` were out-of-bounds, removed, or renamed in the current worktree. Spot checks still resolve at `Sources/SeshatSession/SessionCoordinator.swift:220-241`, `Sources/SeshatAppKit/Composition/SeshatAppMain.swift:170-195`, `Tests/SeshatAppKitTests/ManualNotesVerification.md:11-29`, and `plans/App UI design/Seshat UI Polish & Unified Window Implementation.md:54-57`.
- The staleness problem is omission rather than broken anchors. The plan never cites the already-landed Layer 8 files on trunk: `Sources/SeshatCore/Metrics/MetricsModels.swift:3-89`, `Sources/SeshatCore/Metrics/MetricsProtocols.swift:4-20`, `Sources/SeshatCore/Metrics/SQLiteMetricsReader.swift:4-129`, `Sources/SeshatCore/Metrics/SQLiteMetricsService.swift:4-153`, `Tests/SeshatCoreTests/Metrics/SQLiteMetricsReaderTests.swift:5-138`, and `Tests/SeshatCoreTests/Metrics/SQLiteMetricsServiceTests.swift:6-208`.
