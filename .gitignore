.DS_Store
/.build
/.swiftpm
/Packages
xcuserdata/
DerivedData/
*.xcodeproj
.netrc

# Dev-built app bundle (scripts/package.sh output)
/Ninimma.app/
# DMG output of scripts/package.sh -d
/Ninimma-*.dmg

# Claude Code agent-tool worktrees (isolation: "worktree")
.claude/worktrees/
.codex-heartbeat/

# Scratch / WIP plan documents that are not yet ready to check in.
# Force-add with `git add -f <path>` when a specific file graduates.
plans/App UI design/
plans/backlog/transcript-trigger-context.md
plans/central/STAGE3_HANDOFF.md
plans/central/drafts/
plans/rename/
